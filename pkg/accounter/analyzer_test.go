package accounter

import (
	"testing"
)

func TestAnalyzer(t *testing.T) {
	// analyser := NewAnalyzer(nil, filename)
	// t.Logf("Run test for file %s", filename)
	// count, err := analyser.Pagecount()
	// if err != nil {
	// 	t.Errorf("Error: %q", err)
	// }
	// t.Logf("Size: %d", count)
}
